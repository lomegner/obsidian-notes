
`docker-compose logs`: shows logs of all running container of that application which was connected through docker-container

for seeing each of the container logs, run `docker ps` to grab their container id and then run `docker logs`.
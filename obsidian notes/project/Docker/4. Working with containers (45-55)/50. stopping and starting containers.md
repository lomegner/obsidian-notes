`exit`, closing the terminal, `ctrl+c` will stop the container, for stopping a detached container (container running in the background), we use `docker stop lomejeener`.

for starting a stopped container we use:
`docker start lomejeener`
command above always start the container in the background unless they're interactive which we supply -ai in the command like:
`docker start -ai lomejeener`


when we run our application like a react app, it ports opens on the container ,like we can run this on the browser of the container but of course the browser doesn't have any container so we have to open it on the host machine browser to map this we use -P as we talked earlier in our 
`docker run -it -P 3000:3000 react-app sh` but if we use `EXPOSE` instruction, it will turn into `docker run -P react-app sh` and it will retrieve the the port from the Dockerfile automatically. 

`EXPOSE 3000(port)`= it documents a port  and let it -P uses it later.
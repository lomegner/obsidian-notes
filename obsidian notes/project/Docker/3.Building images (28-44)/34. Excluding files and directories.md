
`build context`= The files Docker can access during build and It's the directory you specify in `docker build .`

`.dockerignore`= excludes files from build context i.e. those files won't appear if you copy the whole root project.

